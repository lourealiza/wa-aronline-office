# WorkAdventure Map Upload Configuration

# Upload strategy: GH_PAGES (GitHub Pages) or MAP_STORAGE (WA Hosted)
UPLOAD_MODE=GH_PAGES

# GitHub Pages configuration (default)
# No additional configuration needed for GH_PAGES mode

# WA Map Storage configuration (uncomment if using MAP_STORAGE)
# MAP_STORAGE_URL=https://map-storage.workadventu.re
# MAP_STORAGE_API_KEY=your-api-key-here
# UPLOAD_DIRECTORY=your-directory-name
